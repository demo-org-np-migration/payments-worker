apiVersion: v1
kind: ConfigMap
metadata:
  name: payments-worker
  labels:
    {{- include "payments-worker.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
